<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:padding="30dp"
    android:layout_height="match_parent">

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GPS Notifications enabled"
        android:paddingBottom="20dp"
        android:id="@+id/checkGPSNotification" />
    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Geofence Alarm Notifications enabled"
        android:paddingBottom="20dp"
        android:id="@+id/checkAlarmNotification" />
    <CheckBox
        android:paddingBottom="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Vibration on notification"
        android:id="@+id/checkVibrate" />
    <CheckBox
        android:paddingBottom="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sound on notification"
        android:id="@+id/checkSound"/>

    <LinearLayout
        android:paddingTop="30dp"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="5"
            android:text="Cancel"
            android:id="@+id/setting_dialog_Cancel"
            android:layout_gravity="center_horizontal" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="5"
            android:text="OK"
            android:id="@+id/setting_dialog_OK"
            android:layout_gravity="right" />
    </LinearLayout>
</LinearLayout>